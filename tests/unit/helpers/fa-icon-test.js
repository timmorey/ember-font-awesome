import { faIcon } from '../../../helpers/fa-icon';
import { module, test } from 'qunit';

module('Unit | Helper | fa icon');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = faIcon(42);
  assert.ok(result);
});
